experiment_id: layernorm_development
study_kind: development
kernels:
  - layernorm
selector_version: v2_validation
shapes:
  - shape_id: layernorm_rows128_hidden768_fp16_rowmajor
    dimensions: {rows: 128, hidden: 768}
    dtype: fp16
    layout: row_major
    workload_class: small_batch
  - shape_id: layernorm_rows128_hidden2048_fp16_rowmajor
    dimensions: {rows: 128, hidden: 2048}
    dtype: fp16
    layout: row_major
    workload_class: small_batch
  - shape_id: layernorm_rows2048_hidden1024_fp16_rowmajor
    dimensions: {rows: 2048, hidden: 1024}
    dtype: fp16
    layout: row_major
    workload_class: large_batch
  - shape_id: layernorm_rows2048_hidden4096_fp16_rowmajor
    dimensions: {rows: 2048, hidden: 4096}
    dtype: fp16
    layout: row_major
    workload_class: large_batch
selector_modes:
  - prune_only
  - prune_rank
  - prune_rank_profiled
  - prune_rank_revised
baselines:
  - default_config
  - naive_random_search
  - naive_grid_search
budgets:
  max_candidates: 12
  max_benchmarks: 10
  max_profiles: 4
  wall_clock_limit_s: 600
calibration_split: 0.5
held_out_split: 0.5
artifact_root: artifacts
counter_set_id: memory_lite
budget_id: layernorm_development_matched_v2
benchmark_settings:
  warmup_iterations: 20
  timed_iterations: 60
  timing_backend: cuda_events
profiling_settings:
  replay_mode: kernel
  timeout_s: 180
  cooldown_s: 1.0
execution_settings:
  isolate_triton_cache: true
  expected_partition: gpunodes
  expected_node_name: gpunode2
  expected_gpu_name: NVIDIA RTX A6000
  cuda_home: /usr/local/cuda-12.9
analysis_settings:
  reportability_target: gpunode2_rtx_a6000
  workload_id: layernorm_representative_dev_v2
seed: 7
notes: Development-sized LayerNorm study for the validation-first contrast workload.
