# Figures Directory

The first draft uses LaTeX-native figure layouts embedded in the section files so the manuscript compiles without a separate plotting pass.

If later draft iterations export polished figures, place the generated assets here and update:

- `paper/FIGURE_MANIFEST.md`
- the figure environments in `paper/sections/results.tex`
- any appendix references that point to LaTeX-native placeholders
