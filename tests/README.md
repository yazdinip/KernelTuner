# Tests

The test tree is pre-created to match the v1 documentation package.

## Layout

- `unit/`: deterministic logic and schema tests
- `integration/`: multi-module tests
- `gpu/`: tests that require the target Linux CUDA host
- `artifacts/`: manifest and schema round-trip tests
